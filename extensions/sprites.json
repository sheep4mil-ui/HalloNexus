{
  "extensionName": "Universal Object, Hitbox & MakeCode Style Sprite Engine",
  "category": "SPRITES",
  "newBlocks": [
    {
      "blockName": "SUMMON SPRITE OF KIND",
      "tooltip": "Instantiates a new active sprite structure in memory and flags its gameplay classification kind.",
      "sideMenuFields": [
        { "label": "Asset Pointer", "type": "text", "default": "PlayerSpriteData" },
        { "label": "Sprite Kind", "type": "text", "default": "0" }
      ],
      "ez80AssemblyTemplate": "ld hl, {AssetPointer}\nld a, {SpriteKind}\ncall _SummonSpriteOfKind"
    },
    {
      "blockName": "SET POSITION",
      "tooltip": "Forces an instant rewrite of an entity's coordinate registers to lock it to specific spatial map positions.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 },
        { "label": "X Pixel", "type": "number", "default": 160 },
        { "label": "Y Pixel", "type": "number", "default": 120 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\nld hl, {XPixel}\nld de, {YPixel}\ncall _SetSpritePosition"
    },
    {
      "blockName": "SET VELOCITY",
      "tooltip": "Injects a continuous movement vector delta that automatically increments spatial coordinates on every clock interrupt pass.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 },
        { "label": "VX Delta", "type": "number", "default": 2 },
        { "label": "VY Delta", "type": "number", "default": 0 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\nld b, {VXDelta}\nld c, {VYDelta}\ncall _SetSpriteVelocity"
    },
    {
      "blockName": "DESTROY SPRITE WITH EFFECT",
      "tooltip": "Wipes an active sprite structure out of system registers and spawns a specific visual particle debris trail pattern.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 },
        { "label": "Effect ID", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\nld b, {EffectID}\ncall _DestroySpriteWithEffect"
    },
    {
      "blockName": "MOVE SPRITE WITH CONTROLLERS",
      "tooltip": "Directly maps physical direction button inputs to an entity's coordinate registers at a set maximum speed step rate.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 },
        { "label": "Max Speed", "type": "number", "default": 4 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\nld b, {MaxSpeed}\ncall _MoveSpriteWithControllers"
    },
    {
      "blockName": "IF OVERLAPPING KIND",
      "tooltip": "Performs continuous proximity bounding-box intersection sweeps between two different sprite kind indices to branch execution logic lanes.",
      "sideMenuFields": [
        { "label": "Kind Alpha", "type": "number", "default": 0 },
        { "label": "Kind Beta", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {KindAlpha}\nld b, {KindBeta}\ncall _CheckSpritesOverlapOfKind\njr nz, _OverlapConditionFailed"
    },
    {
      "blockName": "SET AUTO DESTROY ON WALL",
      "tooltip": "Enforces a lifecycle flag that automatically garbage-collects a projectile or entity memory slot the frame it leaves the screen bounds.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\ncall _SetSpriteAutoDestroy"
    },
    {
      "blockName": "SET BOUNDARY HITBOX",
      "tooltip": "Configures a 4-byte fine-grained environmental boundary offset matrix bypassing standard 16x16 grid constraints.",
      "sideMenuFields": [
        { "label": "Top Offset", "type": "number", "default": 2 },
        { "label": "Bottom Offset", "type": "number", "default": 14 },
        { "label": "Left Offset", "type": "number", "default": 3 },
        { "label": "Right Offset", "type": "number", "default": 13 }
      ],
      "ez80AssemblyTemplate": "ld a, {TopOffset}\nld b, {BottomOffset}\nld c, {LeftOffset}\nld d, {RightOffset}\ncall _ConfigureSubFullBoundaryHitbox"
    },
    {
      "blockName": "RENDER SCREEN FRAME",
      "tooltip": "Loops through all structured RAM object blocks and blits their sprite data matrices onto the screen VRAM buffer.",
      "sideMenuFields": [],
      "ez80AssemblyTemplate": "call _RenderAllActiveEntityPointers"
    },
    {
      "blockName": "SET ANIMATION DELAY",
      "tooltip": "Increments animation loop trackers automatically based on set frame hardware delay parameters.",
      "sideMenuFields": [
        { "label": "Frames Limit", "type": "number", "default": 4 },
        { "label": "Tick Delay", "type": "number", "default": 8 }
      ],
      "ez80AssemblyTemplate": "ld a, {FramesLimit}\nld b, {TickDelay}\ncall _CycleAnimationFrame"
    },
    {
      "blockName": "FLIP HORIZONTAL",
      "tooltip": "Reverses the horizontal rendering orientation of a texture map vector inside double buffers layout frames.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\ncall _FlipTextureHorizontal"
    },
    {
      "blockName": "FLIP VERTICAL",
      "tooltip": "Reverses the vertical drawing layout sequence pass to flip asset textures upside down.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\ncall _FlipTextureVertical"
    },
    {
      "blockName": "SCALE DOUBLE SIZE",
      "tooltip": "Executes a pixel-doubling blit matrix pass to scale rendering sizes cleanly on screen.",
      "sideMenuFields": [
        { "label": "Slot ID", "type": "number", "default": 1 }
      ],
      "ez80AssemblyTemplate": "ld a, {SlotID}\ncall _ScaleSpriteDouble"
    },
    {
      "blockName": "SET TRANSPARENT PALETTE ALPHA",
      "tooltip": "Designates an exact hardware color palette lookup index value as a clear skip parameter for un-boxed boundaries drawing.",
      "sideMenuFields": [
        { "label": "Color Index", "type": "number", "default": 0 }
      ],
      "ez80AssemblyTemplate": "ld a, {ColorIndex}\ncall _SetTransparentPaletteAlpha"
    }
  ]
}
