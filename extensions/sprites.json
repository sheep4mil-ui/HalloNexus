{
  "extensionName": "Sprite Core Pack",
  "category": "Sprites & Screen",
  "newBlocks": [
    {
      "blockName": "SUMMON SPRITE",
      "tooltip": "Allocates memory coordinates and summons a sprite object onto the project grid.",
      "sideMenuFields": [
        { "label": "X Register", "type": "number", "default": 160 },
        { "label": "Y Register", "type": "number", "default": 120 }
      ],
      "ez80AssemblyTemplate": "ld hl, {XRegister}\nld (Player_X), hl\nld de, {YRegister}\nld (Player_Y), de\ncall _SummonActiveSprite"
    },
    {
      "blockName": "SCREEN SHAKE",
      "tooltip": "Triggers a hardware offset shift pass to execute a viewport camera screen shake.",
      "sideMenuFields": [
        { "label": "Intensity", "type": "number", "default": 4 },
        { "label": "Frames", "type": "number", "default": 15 }
      ],
      "ez80AssemblyTemplate": "ld a, {Intensity}\nld b, {Frames}\ncall _ExecuteScreenShakeMatrix"
    },
    {
      "blockName": "DRAW PIXELS",
      "tooltip": "Draws raw color index map data bytes directly onto the active screen buffer frames.",
      "sideMenuFields": [
        { "label": "Data Pointer", "type": "text", "default": "SpriteData" }
      ],
      "ez80AssemblyTemplate": "ld hl, {DataPointer}\ncall _DrawTransparentPixelsMatrix"
    }
  ]
}
