{
  "extensionName": "Hardware Bus, Interrupt & Launcher Core",
  "category": "CONTROLS & HARDWARE BUS",
  "newBlocks": [
    {
      "blockName": "POLL HARDWARE CONTROLLER",
      "tooltip": "Triggers a hardware key scanning array cycle pass and buffers active hardware bits straight into the main register.",
      "sideMenuFields": [],
      "ez80AssemblyTemplate": "call _GetCSC\nld (CachedHardwareInputBits), a"
    },
    {
      "blockName": "ATTACH HARDWARE CLOCK INTERRUPT",
      "tooltip": "Hooks into the hardware bus timer to selectively freeze active physics and AI threads while keeping the UI running.",
      "sideMenuFields": [
        { "label": "Asynchronous Loop State", "type": "text", "default": "Freeze_Physics_Only" }
      ],
      "ez80AssemblyTemplate": "ld a, {AsynchronousLoopState}\ncall _RegisterHardwareClockInterruptBus"
    },
    {
      "blockName": "INITIALIZE MULTI-GAME LAUNCHER",
      "tooltip": "Sets up a secure execution wrapper to launch encrypted sequel binaries using a centralized data AppVar file.",
      "sideMenuFields": [
        { "label": "Target Sequel Binary", "type": "text", "default": "HALLOW2" },
        { "label": "AppVar Storage Title", "type": "text", "default": "NEXUSAV" }
      ],
      "ez80AssemblyTemplate": "ld hl, {TargetSequelBinary}\nld de, {AppVarStorageTitle}\ncall _ExecuteSecureLauncherPayloadWrapper"
    },
    {
      "blockName": "READ ONBOARD CRYSTAL TIMER",
      "tooltip": "Polls the calculator's hardware oscillator timer to output precise loop framerate synchronization parameters.",
      "sideMenuFields": [
        { "label": "Timer Register Delta Save Target Address", "type": "text", "default": "LoopTimerCache" }
      ],
      "ez80AssemblyTemplate": "ld hl, {TimerRegisterDeltaSaveTargetAddress}\ncall _ReadOnboardCrystalTimer"
    },
    {
      "blockName": "SET BUS CPU WAIT STATES",
      "tooltip": "Injects wait states onto the processor clock line to slow down execution cycles for authentic retro hardware loops.",
      "sideMenuFields": [
        { "label": "Wait State Constant Rate", "type": "number", "default": 2 }
      ],
      "ez80AssemblyTemplate": "ld a, {WaitStateConstantRate}\ncall _SetBusCPUWaitStates"
    },
    {
      "blockName": "VERIFY RAM BATTERY POWER",
      "tooltip": "Checks system hardware status lines to guard against core power dumps before executing active filesystem disk saves.",
      "sideMenuFields": [
        { "label": "Low Power Level Warning Switch Flag", "type": "text", "default": "PowerFlag" }
      ],
      "ez80AssemblyTemplate": "ld hl, {LowPowerLevelWarningSwitchFlag}\ncall _VerifyRAMBatteryPower"
    },
    {
      "blockName": "WRITE SAVE STATE SECTOR",
      "tooltip": "Flushes volatile project variables and workspace coordinates snap arrays directly to persistent AppVar archive cells.",
      "sideMenuFields": [
        { "label": "AppVar File Identifier Title String", "type": "text", "default": "NEXUSSV" }
      ],
      "ez80AssemblyTemplate": "ld hl, {AppVarFileIdentifierTitleString}\ncall _WriteSaveStateSector"
    },
    {
      "blockName": "READ SAVE STATE SECTOR",
      "tooltip": "Pulls archived progress metadata out of permanent calculator storage memory blocks right back into project tables.",
      "sideMenuFields": [
        { "label": "AppVar File Identifier Title String", "type": "text", "default": "NEXUSSV" }
      ],
      "ez80AssemblyTemplate": "ld hl, {AppVarFileIdentifierTitleString}\ncall _ReadSaveStateSector"
    },
    {
      "blockName": "CLEAR HARDWARE CALL STACK",
      "tooltip": "Resets the CPU stack pointer register parameters to protect against overflow memory bugs during recursion operations.",
      "sideMenuFields": [
        { "label": "Stack Clear Address Boundary", "type": "text", "default": "StackTop" }
      ],
      "ez80AssemblyTemplate": "ld hl, {StackClearAddressBoundary}\ncall _ClearHardwareCallStack"
    },
    {
      "blockName": "INTERCEPT OS CRASH SYSTEM",
      "tooltip": "Hooks system error trap lines to route unexpected fatal crashes straight back into a clean dashboard fallback recovery loop.",
      "sideMenuFields": [
        { "label": "Recovery Error Redirect Address Pointer", "type": "text", "default": "CrashRecovery" }
      ],
      "ez80AssemblyTemplate": "ld hl, {RecoveryErrorRedirectAddressPointer}\ncall _InterceptOSCrashSystem"
    }
  ]
}
